# 루트 평가 파이프라인(generate_paired_responses.py / judge.py / agreement.py) 의존성.
# agreement.py 는 표준 라이브러리만 쓰므로 아무것도 없이 돈다.
# judge.py 는 --dry-run / --mock 도 의존성 없이 돈다 (anthropic 은 실제 호출 때만 import).
#
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements-eval.txt
#
# api/ (FastAPI 포팅)의 의존성은 별도다 — api/requirements.txt 참조.

anthropic>=0.40,<1.0          # judge.py / generate_paired_responses.py 의 실제 LLM 호출
numpy>=1.26                   # generate_paired_responses.py 리트리벌 벡터 연산
sentence-transformers>=3.0    # E5Embedder. --dummy-embed 로 돌리면 필요 없다
